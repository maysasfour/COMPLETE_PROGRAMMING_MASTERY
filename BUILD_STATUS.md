# Build Status

This file tracks the real, honest state of the repository. It is updated after each module is completed. If a section isn't listed as "Complete" below, assume it is scaffolded at best or not yet started — do not treat folder existence as content existence.

Last updated: 2026-07-16 (session 16).

**Correction history:** an early version of this file falsely claimed the Python course, OOP module, DSA module, and CRUD project were complete when none of them existed on disk (verified via a full `find . -type f` sweep that returned only root docs plus one README). That was corrected, and all four have since been genuinely built and verified in the sessions described below. Do not trust status claims in this file without spot-checking the referenced paths.

## Completed Sections

- **Root documentation**: `README.md`, `ROADMAP.md`, `STUDY_PLAN.md`, `PROGRESS_TRACKER.md`, `VERSIONS.md`, `GLOSSARY.md`, `TECHNOLOGY_COMPARISON.md`, `CONTRIBUTING.md`, `LICENSE`, `INTERVIEW_PREPARATION.md`, this file, plus a root `.gitignore` (excludes `__pycache__/`, `.pytest_cache/`, `node_modules/`, `.env`, etc.).

- **`00-Programming-Fundamentals/`** — complete. Module README plus all 8 lesson subfolders (`01-How-Computers-Run-Programs` through `08-Concurrency-Basics`), each with a README (Beginner/Intermediate/Advanced, real-world usage, Summary, Key Terms, Common Mistakes, Interview Questions, Suggested Next Lesson), a runnable `example.py`, `Exercises/`, and `Solutions/`. 16 `.py` files executed and verified against documented expected output.

- **`01-Languages/Python/`** — complete. Course README + `CHEAT-SHEET.md` + all 22 numbered subfolders (Setup through Mini-Projects), each with README + `example.py` (core lessons 05, 06, 07, 09, 11 also have `Exercises/`/`Solutions/`). Covers setup, syntax, types, operators, control flow, functions, collections, strings, error handling, file handling, OOP, functional concepts, generics/typing, async, modules/packages, sqlite3 database access, `requests`-based API integration, pytest testing, best practices. `20-Exercises`/`21-Solutions` has 7 reconciled problem/solution pairs. `22-Mini-Projects` contains a full command-line expense tracker (sqlite3-backed) with its own test suite. Every `.py` file in the tree (dozens, including the mini-project's CLI and tests) was executed and verified, including a live network call to a public test API for the API-integration lesson.

- **`09-Object-Oriented-Programming/`** — complete. Module README + 8 lesson subfolders (Classes-and-Objects through Generics-and-Static-Members), a top-level `Exercises/` (6 problems) with matching `Solutions/` (6 worked solutions, explanation + runnable code), a `Diagrams/README.md` with 4 valid Mermaid `classDiagram` blocks tied to the mini-project's real classes, and a `Mini-Project/` — a Library Management System (Book/Member/Library, composition + inheritance + polymorphism + encapsulated validated properties) with its own README, source files, and an 11-test suite. All `.py` files executed and verified; test suite passes 11/11.

- **`08-Data-Structures-and-Algorithms/`** — introductory module complete. Module README + 8 fully-built lessons (`01-Complexity-Analysis` through `08-Recursion`: complexity analysis, arrays/strings, linked lists, stacks/queues, hash tables, sorting, searching, recursion), each with README + `implementation.py` + `Exercises/` + `Solutions/`. 5 clearly-labeled "planned, not yet built" stub folders (`09-Trees-and-Binary-Search-Trees` through `13-Greedy-Algorithms`) preview the rest of the eventual curriculum honestly. Stray empty duplicate folders left over from an earlier renumbering pass were identified and removed. Every `implementation.py` and `Solutions/*.py` file was executed and verified; sorting/searching dry-run traces in the READMEs were derived from actual instrumented runs, not generic textbook traces.

- **`22-Projects/Beginner/Task-Management-CRUD/`** — one complete CRUD project. Vanilla HTML/CSS/JS (no build step, no framework), localStorage persistence, task logic extracted into pure functions (`taskLogic.js`) separate from DOM code, so it's genuinely unit-testable. Includes `README.md`, `ARCHITECTURE.md` (Mermaid data-flow + ER diagrams), `TESTING.md` (manual checklist + automated results). Two real Node-runnable test suites (`taskLogic.test.js`, `storage.test.js`) — 29/29 assertions passing, real captured output. Two genuine logic bugs were found and fixed during review (a redundant double-render on edit-save, and whitespace-only titles bypassing validation on the edit form). Additional stack variants (React+Node, Angular+Spring Boot, etc.) are **not yet built** — this satisfies the "one complete CRUD project" deliverable only.

- **`02-Markup-and-Styling/`** — HTML5 and CSS3 lessons complete (not the whole module — see Remaining Sections below). `01-HTML5/` has a full lesson README (semantic elements, forms, tables, media, accessibility, SEO, security), a runnable `example.html`, and an exercise/solution pair (`recipe.html`-style semantic page). `02-CSS3/` has a full lesson README (box model, selectors/specificity, Flexbox, Grid, custom properties + dark mode, transitions/animations, mobile-first responsive design), a runnable `example.html`/`example.css`, and an exercise/solution pair (responsive pricing cards). `Mini-Project/` is a complete one-page responsive portfolio site (`index.html` + `styles.css`) combining every technique from both lessons: semantic landmarks, a skip link, an accessible form, a native-validated contact form, a `auto-fit`/`minmax` responsive Grid gallery, and `prefers-color-scheme` dark mode with zero JavaScript. All 5 `.html` files were parsed with Python's `html.parser` and confirmed to have zero unclosed/mismatched tags; all 3 `.css` files had their `{`/`}` counts verified balanced (16/16, 16/16, 31/31).

- **`01-Languages/JavaScript/`** — core lessons 01–19 complete (Setup through Best Practices — the full course, matching Python's lesson count), following the same structure and depth as the Python course: course `README.md` + `CHEAT-SHEET.md`, each lesson with a full README (objectives → concept → syntax → detailed example → common mistakes → best practices → interview questions) and a runnable `example.js` (or `.mjs` where the lesson specifically demonstrates ES modules). Lessons 05, 06, 07, 08, 09 (Control Flow, Functions, Collections, Strings, Error Handling), and 14 (Async and Concurrency) additionally have an `Exercises/`/`Solutions/` pair. Covers, beyond the lessons already listed in session 3: custom `Error` subclasses and instanceof-based error branching (09); `node:fs/promises` text/JSON file I/O and the `ENOENT` pattern (10); classes with `#private` fields, getters, `extends`/`super`, and `static` members (11); higher-order "decorator" functions, currying, and `pipe`/`compose` (12); an honest treatment of why plain JS has no generics, contrasted with TypeScript (13); the event loop, Promises, `async`/`await`, `Promise.all`/`race`/`allSettled`, and the microtask/macrotask ordering (14, with the concurrency speedup claim measured with real `Date.now()` timing, not just asserted); CommonJS vs. ES Modules, `package.json`, npm, and semantic versioning (15, with working examples of both module systems); SQL CRUD and SQL-injection prevention via Node's built-in `node:sqlite` — zero npm installs (16); the built-in global `fetch()`, including the "`fetch` doesn't throw on 404" trap, verified against a real live call to the public `jsonplaceholder.typicode.com` test API (17); Node's built-in `node:test` runner with `assert/strict`, tested error-throwing, and subtests (18); and a best-practices synthesis contrasting a deliberately-buggy "before" implementation against a corrected "after" one, with the bug (a `||`-based default silently overriding an intentional `0`) reproduced and fixed live in the executed example (19). Every `.js`/`.mjs` file in lessons 09–19 (18 files, plus the 12 from lessons 01–08 covered in session 3) was executed with `node` (or `node --test` for lesson 18's 7 tests) and its actual output verified — no npm installs were needed anywhere in the course, deliberately, by using only built-in Node core modules. Lessons 20–22 (a standalone cross-cutting exercise bank, solutions, and mini-projects) are **not yet built** — honestly marked as such in the course README's table of contents rather than left as empty folders.

- **`01-Languages/TypeScript/`** — core lessons 01–19 complete (Setup through Best Practices — full course, matching Python's and JavaScript's lesson count), explicitly framed as building on top of the JavaScript course rather than re-teaching it: course `README.md` + `CHEAT-SHEET.md`, each lesson with a full README (objectives → concept → syntax → detailed example → common mistakes → best practices → interview questions) and a compilable `example.ts`. Lessons 05, 06, and 07 additionally have an `Exercises/`/`Solutions/` pair. Lessons 01–08 cover `tsc`/type erasure, structural typing, `any`/`unknown`/`never`/`void`, union/literal types, `strictNullChecks`, `as`/`!` vs. type guards, discriminated unions with exhaustive `switch`, function overloads, `Record<K,V>`/`readonly`, and template literal types (all verified in session 5). Lessons 09–19, built this session, cover: `unknown`-typed `catch` and `Result<T,E>` (09); the `JSON.parse` gap and a validated generic `readJsonFile<T>` helper (10); `private`/`protected` (compile-time only, explicitly contrasted with JS's runtime-enforced `#private`) plus `interface implements` and `abstract class` (11); generically-typed higher-order functions preserving exact signatures (12); a full, proper Generics lesson — generic functions/interfaces/classes, `extends` constraints, default type parameters — closing the gap the JS course's Lesson 13 explicitly left open (13); typed `Promise<T>` and `Promise.all`'s tuple-preserving inference (14); type-only imports/exports and `.d.ts`/`@types/` (15); typed-and-validated `node:sqlite` access (16); typed-and-validated `fetch()`, completing a three-lesson "validate at the boundary" arc across files/DB/network (17); `node:test` from TypeScript including a real, reproduced compile-then-run gotcha (18); and a best-practices synthesis naming boundary validation as the course's central recurring theme (19). Every `.ts` file across both sessions (22 total: 11 from session 5, 11 more this session) was compiled with `tsc --strict --target ES2022 --skipLibCheck` (plus `--module commonjs` for Lesson 15's multi-file import/export, and `--esModuleInterop` for Lesson 18's `node:test` default imports) and run with `node`, output verified line-by-line, with zero stray `.js` build artifacts left behind. Multiple claims were independently verified rather than merely asserted: `Record<Role,V>` and exhaustive-switch `never` violations were both reproduced against throwaway files with the exact expected `tsc` error text; a generic constraint violation (`T extends HasLength` rejecting a `number`) was likewise reproduced. Two real bugs were caught and fixed during authoring: a `const name` collision with a global binding in the Strings lesson (session 5), and — this session — Node's test runner double-discovering both `math.test.ts` and its compiled `math.test.js` in the same directory, producing a confusing `ERR_MODULE_NOT_FOUND` that Lesson 18's README now documents as a genuine, reproduced gotcha rather than papering over it. Lessons 20–22 (a standalone cross-cutting exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/CSharp/`** — full core course, lessons 01–19 (Setup through Best Practices), matching Python/JavaScript/TypeScript's depth: course `README.md` + `CHEAT-SHEET.md`, each lesson with a full README and a runnable example. Lessons 01–17 and 19 use .NET 10's **file-based apps** (`dotnet run example.cs`, no `.csproj` needed) to stay single-file, consistent with the rest of this repository's lesson style; Lesson 15 (Modules/Packages) and Lesson 18 (Testing, xUnit) use real multi-file `.csproj` projects since they specifically need multi-file namespaces and NuGet-based test tooling respectively — both deliberate, explained exceptions, not inconsistency. Covers: file-based apps and top-level statements (01); mandatory semicolons and XML doc comments (02); value vs. reference semantics, `var`, nullable reference/value types (03); `??`/`?.`/`is`/`as` (04); `switch` expressions and type-pattern matching with `when` guards, plus an Exercises/Solutions pair (05); `out`/`ref`/`params`/local functions, plus an Exercises/Solutions pair (06); `List<T>`/`Dictionary<K,V>`/LINQ, plus an Exercises/Solutions pair (07); string immutability and `StringBuilder` (08); typed `catch` clauses and exception filters (`when`) (09); `System.IO`/`System.Text.Json`, including a genuinely reproduced .NET-10-file-based-app-specific gotcha (`JsonSerializer` reflection disabled by default, fixed via an `AppContext.SetSwitch` call) documented rather than hidden (10); `virtual`/`override`, `interface`/`abstract class`, and `record` value equality vs. `class` reference equality (11); `Func<>`/`Action<>` delegates and a higher-order wrapper (12); generic methods/classes with `where` constraints, plus a note on .NET's per-value-type specialization vs. Java's full type erasure (13); `async`/`await`/`Task<T>`/`Task.WhenAll` with real `Stopwatch`-measured timing proving the concurrency claim (14); namespaces vs. files vs. assemblies vs. NuGet, using a genuine two-file project specifically to prove namespaces aren't file-bound (15); `Microsoft.Data.Sqlite` CRUD via the .NET-10-only `#:package` file-based-app directive, with the same SQL-injection-safety demonstration as the other language courses (16); `HttpClient` against the same live `jsonplaceholder.typicode.com` API used throughout this repository, including the "doesn't throw on 404" trap and `EnsureSuccessStatusCode()` (17); xUnit `[Fact]`/`[Theory]`/`[InlineData]`/`Assert.Throws<T>` (18); and a best-practices synthesis naming the record-vs-class-equality and null-forgiving-operator footguns as its central examples, both reproduced live (19). Every example across all 19 lessons was actually compiled and run with the .NET 10 SDK (`dotnet run`, or `dotnet test` for Lesson 18), output verified, and all `obj`/`bin` build artifacts cleaned up afterward (also added to `.gitignore`). Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Java/`** — full core course, lessons 01–19 (Setup through Best Practices), matching Python/JavaScript/TypeScript/C#'s depth: course `README.md` + `CHEAT-SHEET.md`, each lesson with a full README and a runnable `Example.java`. Lessons 01–14, 17, and 19 use JDK 11+'s **single-file source launcher** (`java Example.java`, no `javac`/build step) to stay single-file, consistent with this repository's lesson style; Lesson 15 (packages) uses a genuine multi-file/multi-package project specifically to demonstrate that — unlike C#'s location-independent namespaces — a Java package name is *enforced* to match its directory path; Lessons 16 (JDBC/SQLite) and 18 (JUnit 5) require external JARs (a database driver, a test framework) downloaded on demand via `curl` and documented in each lesson's README, never committed to the repo (added to `.gitignore` alongside `*.class`), exactly like `node_modules`/NuGet/PyPI dependencies aren't committed for the other language courses. Covers: single-file execution and top-level-statement-free syntax — Java has no free functions at all, unlike every other language course so far (01–02); primitives vs. boxed reference types, `var`, and the boxed-`Integer` caching equality gotcha, reproduced live (03); the `==`-vs-`.equals()` distinction and string interning, also reproduced live, established here as the course's central recurring theme (04); switch expressions and pattern-matching `switch` (Java 21+), plus an Exercises/Solutions pair that surfaced and documented a real compiler error (primitive patterns being preview-only, fixed by switching on a boxed `Integer`) (05); methods-only (no free functions), overloading as the substitute for default parameters, varargs, plus an Exercises/Solutions pair (06); the Collections Framework and a first look at Streams, plus an Exercises/Solutions pair using `Collectors.groupingBy`/`Comparator` composition (07); string immutability, `StringBuilder`, text blocks (08); checked vs. unchecked exceptions — a genuinely distinctive Java design choice not present in any other language course here — and try-with-resources (09); `java.nio.file.Files`, and an explicit, honest flag that the JDK has **no built-in JSON library at all** (unlike Python/Node/C#), unlike every other data-boundary lesson in this repository (10); overridable-by-default polymorphism (the inverse of C#'s opt-in `virtual`), interface default methods, and `record` value equality (11); built-in functional interfaces (`Function`/`Predicate`/`Consumer`/`Supplier`), method references, and Streams in depth (12); generics with full type erasure, bounded type parameters, and PECS wildcards, explicitly contrasted with C#'s reified generics (13); `CompletableFuture`, `allOf` with real-timing-verified concurrency, and Java 21+ virtual threads (14); packages tied to directory structure, JARs, Maven/Gradle (15); JDBC CRUD via a downloaded `sqlite-jdbc` driver JAR, with the same SQL-injection-safety demonstration as every other language course (16); the built-in (since Java 11) `java.net.http.HttpClient` against the same live `jsonplaceholder.typicode.com` API used throughout this repository, with an explicit, honest reminder that real JSON parsing still needs Jackson/Gson (17); JUnit 5 `@Test`/`@ParameterizedTest`/`@CsvSource`/`assertThrows` via the JUnit Platform Console Standalone JAR (18); and a best-practices synthesis centering the `==`-vs-`.equals()` gotcha as Java's single most consequential recurring footgun, reproduced live one final time (19). Every example across all 19 lessons was actually compiled and run with a real JDK (JDK 25 in this environment; `java Example.java` for single-file lessons, `java Main.java` for Lesson 15, `javac`+`java -jar junit.jar` for Lesson 18), output verified, and all `.class`/downloaded `.jar` artifacts cleaned up afterward. Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Cpp/`** — full core course, lessons 01–19 (Setup through Best Practices), matching Python/JavaScript/TypeScript/C#/Java's depth: course `README.md` + `CHEAT-SHEET.md`, each lesson with a full README and a runnable `example.cpp`. Compiled and run throughout with real MSVC (Visual Studio 2026, `cl.exe` via a `vcvars64.bat`-initialized environment, `/std:c++20`), since no g++/clang++ was available in this environment — a genuine MSVC-specific gotcha was hit and fixed in the process (see below). Lessons 01–14 and 19 are single-file; Lesson 15 uses a genuine multi-file header/source split (`mathutils.hpp`/`.cpp`/`main.cpp`) to demonstrate declaration/definition separation and the linker's role; Lessons 16–18 use external dependencies (SQLite's amalgamation, the cpp-httplib single-header HTTP library, Catch2's amalgamated test framework) downloaded on demand and never committed, exactly like the JDBC/JUnit JARs in the Java course — all covered by an expanded `.gitignore` (`*.exe`, `*.obj`, `*.pdb`, `*.ilk`, `*.jar`, `*.class`, `bin/`, `obj/`). This course leans harder on genuinely new-to-this-repository C++-specific concepts than any prior language, since C++ diverges from every other course's shared "everything except primitives is a reference" assumption: value semantics by default (01–08), explicit `virtual` plus the **slicing** problem unique to by-value polymorphism (11), templates as compile-time code generation contrasted explicitly with Java's erasure and C#'s reification (13), RAII as the *replacement* for a `finally` keyword C++ doesn't have (09), and smart pointers/the Rule of Zero as modern replacements for manual `new`/`delete` (19). Specific lesson content: single-file source launcher and the compile-then-link pipeline, including a genuinely reproduced and fixed MSVC quirk — `__cplusplus` reporting the pre-C++11 value `199711L` even under `/std:c++20` unless `/Zc:__cplusplus` is also passed (01); no top-level statements, `#include`/preprocessor mechanics (02); value-copy-by-default for objects (not just primitives), references vs. pointers, `auto`/`const` (03); `nullptr`, pointer arithmetic and its undefined-behavior risk (04); switch fall-through, range-based `for` with `const auto&`, structured bindings, plus an Exercises/Solutions pair (05); free functions (unlike Java), default arguments, pass-by-value/reference/const-reference, plus an Exercises/Solutions pair proving the by-value-vs-by-reference distinction live (06); STL containers, `operator[]` (no bounds checking) vs. `.at()` (throws), `<algorithm>`, plus an Exercises/Solutions pair (07); `std::string`'s **mutability** — a deliberate, explicit contrast with every other language course's immutable strings — and `std::string_view` (08); typed `catch(const&)`, checked-vs-unchecked is a Java-only concept here (C++ has one exception model), and RAII replacing `finally` (09); `<fstream>`'s non-exception-based missing-file handling (a genuine, distinct convention from every other file-handling lesson) and the same no-built-in-JSON gap as Java (10); explicit `virtual`/`override`, mandatory virtual destructors for polymorphic bases, and slicing reproduced live as a genuine bug (11); lambda capture-by-value vs. capture-by-reference (a C++-specific concern with no garbage collector to paper over dangling references) and `std::function` (12); templates with concepts (C++20), contrasted explicitly with Java/C# generics as a third, distinct model (13); `std::thread` (must `.join()`/`.detach()` or the program terminates), `std::async`/`std::future` with real-timing-verified concurrency, and `std::mutex`/`std::lock_guard` (14); the header/source split, include guards, namespaces, and the honest note that C++ has no single standard build tool/package manager (CMake/vcpkg/Conan are the closest things), demonstrated with a genuine two-file project (15); the SQLite C API used directly (no even Java-JDBC-style abstraction — C++ has literally zero built-in database access), with the same SQL-injection-safety demonstration as every other language course (16); cpp-httplib against the same live `jsonplaceholder.typicode.com` API used throughout this repository (over plain HTTP specifically to avoid an OpenSSL dependency, after a local-server approach was tried, hit what appeared to be a Windows-firewall-prompt hang, and was deliberately abandoned in favor of the simpler, verified live-client approach) (17); Catch2's `TEST_CASE`/`REQUIRE`/`GENERATE`/`REQUIRE_THROWS_AS` (18); and a best-practices synthesis contrasting raw `new`/`delete` with `std::unique_ptr`/`std::shared_ptr` and the Rule of Zero, run and verified live (19). Every example was actually compiled and run, output verified line-by-line, with all `.exe`/`.obj`/downloaded dependency files cleaned up afterward — none left in the repository. Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Go/`** — full core course, lessons 01–19 (Setup through Best Practices), matching every prior language course's depth. The Go SDK was not present in this environment and was downloaded and extracted directly (Go 1.23.4, no system install) specifically for this session — `go run main.go` used throughout for single-file lessons (01–14, 19), with `go.mod`/`go.sum` genuinely committed (not deleted like the Java JDBC JAR / C++ dependencies) for Lessons 15, 16, and 18, since Go's module files are small, legitimate, version-controllable text files by design, not the dependency code itself. Go required the most real topic adaptation of any language yet, given how deliberately it diverges from every other course here: no classes/inheritance (structs + implicit interfaces + embedding instead, Lesson 11), no exceptions (the `(value, error)` return pattern instead, Lesson 09, enabled directly by multiple return values, Lesson 06), no generics until 2022 (Lesson 13), and goroutines/channels as a genuinely distinct concurrency model (Lesson 14) rather than threads or async/await. Specific content: the single-file source launcher and Go's fully static, dependency-free compiled binaries (01); mandatory same-line braces as a hard consequence of automatic semicolon insertion, not just style (02); zero values eliminating uninitialized-variable bugs entirely, `var` vs. `:=` (03); no operator overloading and no ternary operator — both deliberate simplicity choices (04); `switch` NOT falling through by default (the opposite of C/JS/C++), Go's single unified `for` loop keyword, plus an Exercises/Solutions pair (05); multiple return values as the feature that makes Go's whole error-handling idiom possible, named returns, variadic parameters, plus an Exercises/Solutions pair (06); arrays vs. slices, and slice aliasing reproduced live as a genuine, concrete gotcha (mutating a sub-slice visibly changed the original), plus an Exercises/Solutions pair (07); strings as immutable byte sequences with a real byte-vs-rune distinction proven live (a 2-byte UTF-8 character causing a `len()` vs. actual-character-count mismatch, and a rune-iteration byte-offset jump from 1 to 3) (08); the `(value, error)` pattern in depth, custom errors via `errors.As`, and `panic`/`recover` reserved for genuinely exceptional cases (09); `os`/`encoding/json` — Go, unlike Java/C++, has genuine built-in JSON support (10); structs+methods+implicit interfaces+embedding as Go's complete, deliberate non-OOP alternative to classes/inheritance (11); first-class functions, closures, and the Go 1.22+ loop-variable-capture fix, verified live to confirm modern semantics (12); type parameters (Go 1.18+), `comparable`/`cmp.Ordered` constraints, explicitly framed as a much more recent addition than every other language course's generics (13); goroutines, channels, `sync.WaitGroup`, and `select`, with real timing verifying the concurrency claim (14); directory-based packages (unlike C#'s namespaces, like Java) and modules, with capitalization as the *only* visibility mechanism (15); `database/sql` plus the pure-Go `modernc.org/sqlite` driver (chosen specifically to avoid a CGO/C-toolchain dependency), with the same SQL-injection-safety demonstration as every other language course (16); the built-in `net/http` client against the same live `jsonplaceholder.typicode.com` API used throughout this repository (17); the built-in `testing` package and Go's table-driven-test idiom, needing no special parameterization syntax at all (18); and a best-practices synthesis whose two live-reproduced bugs — a silently-ignored error, and an unsynchronized-goroutine counter that read `990` instead of `1000` before the fix — anchor Go's two most consequential recurring themes (19). Every example was actually run with the Go toolchain (`go run`/`go test`), output verified line-by-line, with zero stray build artifacts left in the repository (`go run` leaves none by design). Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Rust/`** — full core course, lessons 01–19 (Setup through Best Practices), matching every prior language course's depth. The Rust toolchain was not present in this environment and was installed via `rustup-init.exe` directly into a scratchpad-local `RUSTUP_HOME`/`CARGO_HOME` (Rust 1.97 stable), never a system-wide install. Rust required the deepest topic adaptation of any course so far, since its central concept — ownership/borrowing enforced entirely at compile time — has no equivalent in any prior course (every other language here either garbage-collects or, in C++'s case, leaves memory safety to convention rather than compiler proof). Specific content: single-file `rustc`/`cargo` compilation (01); expression-oriented syntax where `if`/`match`/blocks are expressions, and semicolons control statement-vs-expression status (02); ownership and moves, reproduced live against a real `E0382` "borrow of moved value" compiler error (03); the borrow-checker's "many immutable XOR one mutable" reference rule, no `null` (`Option<T>` instead), no implicit numeric conversion, and integer overflow's debug-vs-release-mode difference (`wrapping_add`) (04); exhaustive `match` (compiler-enforced, stronger than any prior course's switch), `loop`/`while`/`for`, and `loop { break value; }`, plus an Exercises/Solutions pair (05); ownership-aware function parameters (owned `T` vs. `&T` vs. `&mut T`), plus an Exercises/Solutions pair (06); `Vec<T>`/`HashMap<K,V>`, `.get()`'s `Option`-returning safe indexing vs. panicking `[i]` indexing, iterator adapters (`map`/`filter`/`fold`), plus an Exercises/Solutions pair (07); `String` (owned) vs. `&str` (borrowed) — reproduced live against a real `E0277` compiler error for direct string indexing — byte length vs. `.chars().count()` (08); `Result<T,E>`/`?`, custom error types via `impl std::error::Error`, and `panic!` with no general-purpose `recover` (an explicit contrast with Go's `recover`) (09); `std::fs` plus the same no-built-in-JSON gap as Java/C++, needing `serde`/`serde_json` (10); structs+`impl`+explicit trait implementation (contrasted with Go's implicit interfaces), trait default methods, and composition-only with no embedding/field promotion (11); closures with `Fn`/`FnMut`/`FnOnce` and the `move` keyword (12); generics via monomorphization — a separate compiled copy per concrete type, explicitly contrasted with Java's erasure and C#'s reification — plus trait bounds (13); `std::thread::spawn` plus `Arc<Mutex<T>>`, Rust's compile-time-enforced "fearless concurrency" explicitly contrasted with Go's runtime-only race detector and C++'s convention-only mutex discipline, and `async`/`await` syntax noted as needing an external runtime crate (not built, matching this course's dependency-minimal style) (14); `mod`/`pub`, crates vs. packages, and `Cargo.toml`/`Cargo.lock` (genuinely committed, like Go's `go.mod`/`go.sum`) (15); `rusqlite` with its `bundled` feature (compiles SQLite from C source via MSVC automatically, no system SQLite needed), with the same SQL-injection-safety demonstration as every other language course (16); the `ureq` crate for HTTP (Rust's std has zero networking support, like C++) combined with `serde`/`serde_json` for JSON — and a genuinely new, verified finding: `ureq` treats any non-2xx response as `Err(ureq::Error::Status(code, response))` **by default**, a real exception to the "4xx/5xx resolves as a normal response" pattern established by every other language course in this repository (Python, JS, TS, C#, Java, Go, C++), confirmed by actually running the code against a live 404 response, not assumed from documentation (17); the built-in `#[test]` attribute and `cargo test`, `#[cfg(test)]`, table-driven-style tests via tuple arrays, `#[should_panic]`, `Result`-returning tests, and the genuine distinction between inline unit tests (private-item access via `use super::*`) and `tests/`-directory integration tests (public-API-only, each file its own crate) (18); and a best-practices synthesis reproducing three genuine anti-pattern/fix pairs live — needless `.clone()` vs. borrowing, `.unwrap()` vs. `Result`/`?` propagation, and repeated `+` string concatenation vs. `String::with_capacity`/`push_str` — with a passing `assert_eq!` confirming the "bad" and "good" versions produce byte-identical output (19). Every example across all 19 lessons was actually compiled/run with the real Rust toolchain (`rustc` for single-file lessons, `cargo run`/`cargo test` for Lessons 16–18), output verified line-by-line against live network calls where applicable (the same `jsonplaceholder.typicode.com` test API used throughout this repository), and all `target/` build directories cleaned up afterward (already covered by `.gitignore`). Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/PHP/`** — full core course, lessons 01–19 (Setup through Best Practices), matching every prior language course's depth. PHP was not present in this environment and was installed by downloading the official Windows CLI zip build (PHP 8.4.23) directly into a scratchpad location, never a system-wide install; `pdo_sqlite`, `sqlite3`, `curl`, `openssl`, `json`, and `mbstring` extensions were enabled in `php.ini`, and a Mozilla CA certificate bundle (`cacert.pem`, from curl.se) had to be downloaded and wired up via `curl.cainfo`/`openssl.cafile` before any HTTPS request would succeed at all — a genuine, reproduced environment-setup gotcha, not a code bug. PHPUnit was downloaded as a standalone `.phar` (`phpunit-11.phar`) for Lesson 18, requiring no Composer install, matching this repository's pattern of downloading JUnit/Catch2 as standalone artifacts for the Java/C++ courses. Specific content: interpreted execution with no build step at all (01); a genuine, live-reproduced gotcha where a literal `?>` sequence inside a `//` comment ended PHP mode immediately, breaking the file mid-comment (02); dynamic typing and PHP 8's real, versioned fix to `0 == "abc"` loose comparison (03); the spaceship (`<=>`) and null-safe (`?->`) operators, plus a live-reproduced `and`-vs-`=` operator-precedence trap (`$a = true and false` assigns `true`, not `false`) (04); `switch`'s C-style fall-through contrasted with PHP 8's `match` (strict comparison, expression-based, no fall-through), plus an Exercises/Solutions FizzBuzz pair (05); named arguments, variadic parameters, by-reference (`&$param`) parameters genuinely mutating the caller's variable, arrow-function auto-capture vs. anonymous-function explicit `use()`, and first-class callable syntax, plus an Exercises/Solutions pair (06); PHP's single `array` type serving as both list and ordered map, with a live-verified `array_filter()`-preserves-original-keys gotcha, plus an Exercises/Solutions pair (07); core vs. `mb_*` string functions (byte vs. character length, verified live with a real multibyte string), heredoc/nowdoc (08); `try`/`catch`/`finally`, custom exceptions with constructor property promotion, PHP 8 multi-catch, and the genuinely distinctive split `Error`/`Exception` hierarchy (both implementing `Throwable`), verified live via `ArgumentCountError extends Error` (09); `file_get_contents`/`fopen`, PHP's non-exception-based (`false`-returning) file-I/O error convention verified live, and PHP's genuinely built-in JSON support (10); classes/interfaces/abstract classes, traits as PHP's answer to no multiple inheritance, and PHP 8.1+ backed enums with methods (11); closure capture-by-value-vs-by-reference (`use (&$var)`), verified live to produce genuinely different behavior, higher-order functions, and composition (12); an honest treatment of PHP having NO generics at all, covering `mixed` (no safety), PHPDoc `@template` (static-analysis-only via PHPStan/Psalm, zero runtime effect), and interface-based constraints as the only runtime-enforced option — including a genuine, live-reproduced gotcha where `self`-typed interface/implementation parameters still failed PHP's invariant-parameter-type check (13); Fibers (PHP 8.1+, cooperative multitasking, verified live via suspend/resume, explicitly NOT parallelism) and `curl_multi_*` for genuine concurrent I/O, with a real ~7x speedup measured directly against the live test API (14); namespaces, `spl_autoload_register`-based PSR-4 autoloading demonstrated by hand (Composer itself unavailable in this environment), and a live-confirmed non-gotcha (PHP's `use` import statement resolves independent of its position in the file) (15); PDO CRUD against SQLite with named parameterized placeholders, the same SQL-injection-safety demonstration as every other language course (16); `curl`/`file_get_contents` against the same live `jsonplaceholder.typicode.com` API used throughout this repository, confirming no exception is thrown for a 404 (matching every other language course's HTTP client convention), plus the genuine CA-bundle environment-setup gotcha documented above (17); PHPUnit `#[Test]`/`#[DataProvider]`/`expectException`, downloaded as a standalone `.phar` (18); and a best-practices synthesis reproducing three genuine, live anti-pattern/fix pairs — `"0" == 0` remaining `true` even on PHP 8 (a real gap `===` closes), a real, successful SQL injection against a string-concatenated query versus a safely-rejected parameterized one, and an unchecked `file_get_contents()` failure surfacing as a confusing, unrelated `TypeError` versus a checked version's clear `RuntimeException` (19). Every example across all 19 lessons was actually run with the real PHP CLI, output verified line-by-line against live network calls where applicable, and no stray scratch files or `.phar` artifacts left in the repository (`.gitignore` updated with `*.phar`/`vendor/`). Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Kotlin/`** — full core course, lessons 01–19 (Setup through Best Practices), matching every prior language course's depth. The Kotlin compiler was not present in this environment; the first attempt (Kotlin 2.1.0) failed outright on the pre-installed JDK 25 with `IllegalArgumentException: 25.0.3` thrown from its bundled JDK-version-string parser — a genuine, reproduced compiler/JDK compatibility issue, resolved by downloading the newer Kotlin 2.4.10 release instead, which worked cleanly. External JARs (Gson for JSON, `kotlinx-coroutines-core` for coroutines, `sqlite-jdbc` for database access, `kotlin-test`/`kotlin-test-junit5` plus `junit-platform-console-standalone` for testing) were all downloaded directly, matching this repository's established pattern of never committing such dependencies (`.gitignore` already covered `*.jar`/`*.class` from the Java course). A genuine environment quirk was hit and worked around in Lesson 18: `kotlinc.bat`'s handling of a multi-entry, semicolon-separated `-cp` argument passed directly on the command line mis-split it (later classpath entries were misinterpreted as source-file arguments); an `@argfile` (arguments in a text file, invoked as `kotlinc @args.txt`) sidestepped the issue cleanly. Kotlin's central, most consequential feature — null safety enforced by the type system itself — was verified live via a real compile error (`error: null cannot be a value of a non-null type 'String'`), and several further genuine, verified language-design contrasts with this repository's existing Java course anchor nearly every lesson. Specific content: JVM interoperability, `kotlinc`, and the compiler/JDK compatibility fix (01); top-level functions with no enclosing class required, `val`/`var`, string templates (02); null safety (`String` vs `String?`) verified live via a real compile error, `?.`/`?:`/`!!` (03); `==` (structural) vs `===` (referential) — verified live to be the exact **opposite** of Java's convention, plus a live-reproduced JVM string-interning surprise where `===` returned `true` for two separately-written string expressions, and ranges (04); `if`/`when` as expressions, `when`'s no-fall-through design, and `sealed class` + exhaustive `when` (compiler-enforced, comparable to Rust's exhaustive `match`), plus an Exercises/Solutions FizzBuzz pair (05); named/default parameters, `vararg`, extension functions (a genuinely distinctive Kotlin feature resolved statically, not dynamically — verified via its receiver-type-based resolution), trailing lambda syntax and `it`, plus an Exercises/Solutions pair (06); read-only (`List`) vs. mutable (`MutableList`) collection interfaces, and a genuinely important, live-verified nuance that read-only is NOT the same as immutable (a read-only-typed view of a mutable object still reflects external mutations), plus an Exercises/Solutions pair (07); triple-quoted raw strings with `trimIndent()`, and Kotlin's `String` being literally `java.lang.String` underneath, with the structural-vs-referential contrast revisited and shown to behave differently for a runtime-constructed string vs. Lesson 04's compile-time-folded literal (08); `try` usable as an expression (a genuine Kotlin feature Java's `try` statement lacks), and Kotlin's deliberate, verified design choice to have **no checked exceptions at all** (demonstrated by calling Java's checked `IOException` with no `throws` declaration needed or even possible) (09); `File` extension functions (`writeText`/`readText`/`appendText`/`forEachLine`) with Java-matching exception-based error handling (verified live via `FileNotFoundException`, contrasted with PHP's `false`-returning convention), and the same no-built-in-JSON gap as Java, requiring Gson (10); classes and methods **final by default** — verified live via a real "this type is final, so it cannot be extended" compile error, the opposite of Java's default — `data class` auto-generated equality/copy, `object` singletons, and companion objects as Kotlin's real, object-based replacement for `static` (11); closures that CAN mutate captured local `var`s (verified live with a genuinely stateful counter-factory closure), contrasted explicitly with Java's effectively-final lambda-capture restriction, function references (`::`), and the `let`/`apply` scope functions (12); a real, meaty generics lesson covering declaration-site variance (`out`/`in`) verified live to succeed where Java's use-site wildcards would need repeating at every call site, plus `reified` type parameters (only on `inline fun`) enabling genuine runtime `is T` checks impossible with ordinary erased generics (13); Kotlin coroutines (`suspend`, `kotlinx.coroutines` as a separate library, mirroring Rust's `async`/`tokio` split), with `async`/`await`'s concurrency genuinely measured (413ms sequential vs. 212ms concurrent for two 200ms-delayed operations) and structured concurrency proven via output ordering (a `coroutineScope` doesn't return until its children finish, even though `launch` itself doesn't block) (14); packages verified live to be entirely independent of directory structure in Kotlin — a real, checked contrast with Java's enforced package-directory matching — and Gradle's role as the standard build tool (15); JDBC CRUD (identical API to the Java course, since Kotlin uses `java.sql` directly) with `.use { }` as Kotlin's concise try-with-resources equivalent, and the same SQL-injection-safety demonstration as every other language course (16); `java.net.http.HttpClient` (used directly from Kotlin, no separate networking library needed) against the same live `jsonplaceholder.typicode.com` API used throughout this repository, confirming no exception is thrown for a 404 (matching every language course's HTTP client except Rust's `ureq`) (17); `kotlin.test` (a framework-agnostic assertion API) bound to JUnit 5 via `kotlin-test-junit5`, including the genuine classpath-splitting gotcha documented above (18); and a best-practices synthesis reproducing three genuine anti-pattern/fix pairs live — `!!` producing an unhelpful, message-less `NullPointerException` versus a clear, specific exception via `?:`; a class exposing its mutable backing list directly (genuinely mutated from outside, verified live) versus returning an actual defensive copy; and `Thread.sleep()` inside a coroutine measurably defeating cooperative scheduling (412ms) versus `delay()` (208ms), both timings measured directly (19). Every example across all 19 lessons was actually compiled and run with the real Kotlin compiler and JDK 25, output verified line-by-line against live network calls and real measured timing where applicable, and all `.jar`/`.class`/`out` build artifacts cleaned up afterward (already covered by the existing `.gitignore`). Lessons 20–22 (a standalone exercise bank and mini-projects) are **not yet built**.

- **`01-Languages/Swift/` — ⚠️ UNVERIFIED, a genuine, disclosed exception to this repository's core discipline.** Full course structure exists, lessons 01–19 (Setup through Best Practices), matching every prior language course's *depth of writing* — but **no example in this course was compiled or run**. Swift's official Windows toolchain ships only as a large (~570MB), system-wide InstallShield `.exe` installer, unlike the self-contained zip/archive toolchains used for Go, Rust, PHP, and Kotlin (all of which installed cleanly into an isolated, scratchpad-local directory this session and previous ones, with zero lasting footprint). A silent-install attempt (`/quiet`, then `/s /v/qn`) was tested; the first produced no installed toolchain at all (verified: no `swift`/`swiftc` on PATH, no `C:\Program Files\Swift` directory), and the second attempt was blocked by this environment's auto-mode safety classifier specifically because it constituted an unauthorized, persistent, machine-wide system change (Program Files/registry) with no interactive confirmation available — a materially different risk category than every previous toolchain install this session and prior sessions performed. The user was asked directly via `AskUserQuestion` how to proceed (skip Swift for now / authorize a system-wide install / write the course unverified from documented knowledge) and explicitly chose the third option. Every lesson's README prominently displays a repeated honesty notice (both in the course-level `README.md` and at the top of every individual lesson) stating plainly that the examples were not executed, and the course README explicitly asks anyone with a working Swift toolchain to compile/run the examples and treat discrepancies as this course's error, not the language's. Content covers, per standard documented Swift semantics: LLVM-based compilation and `swiftc` (01); top-level code, `let`/`var`, string interpolation (02); Optionals (`T?`), `?.`/`??`/`!`, and `guard let`/`if let` — framed as the direct Swift analogue to the Kotlin nullable-types course covered immediately prior in this repository (03); no implicit numeric conversion (matching Rust/Kotlin), `Equatable` auto-synthesis, custom operator overloading (04); `switch` with no fall-through by default (matching Go), range/tuple/`where`-clause pattern matching, plus an Exercises/Solutions FizzBuzz pair (05); Swift's genuinely distinctive argument-label-vs-parameter-name split, `inout` parameters requiring an explicit `&` at the call site, trailing closures and `$0`, plus an Exercises/Solutions pair (06); `Array`/`Dictionary`/`Set` as genuine VALUE types (copied on assignment, no aliasing) — explicitly contrasted with the exact Kotlin Lesson 07 aliasing finding from this repository's Kotlin course, since Swift's value-type collections structurally cannot exhibit that same bug — plus an Exercises/Solutions pair (07); Unicode-correct, grapheme-cluster-based `String.count` (O(n), not O(1)), demonstrated conceptually with a multi-scalar emoji, and `String.Index`-based (not integer) indexing (08); the `Error` protocol, `throws`/`try`/`try?`/`try!`, framed as conceptually similar to Rust's `Result<T,E>` but with propagation-syntax rather than a wrapped return type (09); `FileManager`-based file I/O (exception-based, matching Kotlin/Java) and `Codable`/`JSONEncoder`/`JSONDecoder` — framed as a genuine, positive contrast, since Java/Kotlin/C++/Rust all needed an external JSON library in their own courses in this repository (10); `struct` (value type, the idiomatic default) vs. `class` (reference type, used deliberately) as Swift's most fundamental OOP design choice — with no equivalent decision in Kotlin/Java, where everything is a reference-type class — plus protocol-oriented programming (protocol extensions providing default implementations) and enums with associated values (comparable to Rust's `enum`) (11); mutable closure capture (matching Kotlin, contrasted with Java) and capture lists (`[weak self]`) as a preview of ARC/retain-cycle concerns (12); generic constraints and protocols with associated types, a mechanism comparable to Rust's traits with associated types and more flexible in some respects than Java's type-erasure-based generics, plus `mutating` methods as a consequence of struct value semantics (13); native `async`/`await`/`async let`, framed as a genuine, positive contrast since Kotlin needs the separate `kotlinx.coroutines` library and Rust needs the separate `tokio` crate for the equivalent capability, and `actor` for compiler-enforced, data-race-free mutable state — a stronger guarantee than Rust's `Arc<Mutex<T>>` (14); Swift's five-level access control system (`private`/`fileprivate`/`internal`/`public`/`open`), with the genuinely distinctive `public`-vs-`open` split (cross-module visibility vs. cross-module subclassing) having no equivalent in Kotlin or Java, and Swift Package Manager (`Package.swift`) (15); no built-in database access (matching C++'s gap), demonstrated via the raw SQLite3 C API and `OpaquePointer` (16); `URLSession` with native `async`/`await` against the same `jsonplaceholder.typicode.com` API pattern used throughout this repository (not actually called, per the disclosed limitation), and `Codable` for JSON (17); `XCTest`, bundled directly with the toolchain — a genuine convenience over the separately-downloaded testing libraries needed in this repository's Kotlin/PHP/Java courses (18); and a best-practices synthesis covering force-unwrap crash risk, `class`-vs-`struct` aliasing, and retain cycles, with the retain-cycle anti-pattern's demonstration honestly noting it isn't observable via simple console output at all (needing a memory profiler), a genuine limitation disclosed rather than glossed over (19). `CHEAT-SHEET.md` and this course's `README.md` both carry the same prominent honesty notice. No `.gitignore` changes were needed since nothing was compiled (no build artifacts exist to exclude). Lessons 20–22 are not built, consistent with every other language course.

- **`01-Languages/Dart/`** — full core course, lessons 01–19 (Setup through Best Practices), matching every prior language course's depth, **fully verified by execution** (Dart was already installed in this environment via an existing Flutter SDK at `C:\Users\HP\flutter`, requiring no fresh toolchain install). This completes the original brief's full specified 12-language build order (Python, JavaScript, TypeScript, C#, Java, C++, Go, Rust, PHP, Kotlin, Swift, Dart). External packages (`sqlite3` for Lesson 16, `test` for Lesson 18) were resolved via `dart pub get`, matching this repository's established pattern of downloading-not-committing dependencies; `.dart_tool/`/`.packages`/`build/` were added to `.gitignore`. Specific content, several lessons drawing direct, verified three-way comparisons against the immediately-preceding Kotlin and Swift courses: Dart VM (JIT) vs. AOT compilation, `dart run` (01); Dart's required `void main()` entry point (unlike Kotlin/Swift's top-level statements) and mandatory semicolons, with `final`-reassignment verified live to produce a real compile error (02); sound null safety (`String` vs `String?`), with the non-nullable-assignment compile error AND the force-unwrap runtime crash both verified live, and `late` for deferred initialization (03); `~/` dedicated integer division and the cascade operator (`..`) — verified live to configure multiple properties/methods on one object and evaluate to that original object, a genuinely distinctive feature with no equivalent in any other language covered in this repository (04); `switch` verified live to NOT fall through by default (matching Go/Swift, opposite of C/Java/JS), the explicit `continue`-label fallthrough mechanism also verified live, and Dart 3 switch expressions/pattern matching, plus an Exercises/Solutions FizzBuzz pair (05); named vs. optional-positional parameters, and mutable closure capture verified live (matching Kotlin/Swift, contrasted with Java), plus an Exercises/Solutions pair (06); collection-if/collection-for in literals (verified live, a genuinely distinctive Dart feature), and `List.unmodifiable()` verified live to provide genuine, independent immutability — explicitly contrasted with the exact Kotlin Lesson 07 aliasing finding from this repository's Kotlin course — plus an Exercises/Solutions pair (07); `String.length` verified live to count UTF-16 code units (matching Java/JavaScript) via a multi-scalar emoji, explicitly contrasted with Swift's grapheme-cluster-based `.count` from the immediately-preceding course (08); `try`/`catch`/`on`/`rethrow`, with Dart's genuinely permissive "any object can be thrown" design (no required `Throwable` base type, unlike Kotlin/Java/PHP) and the `RangeError`-is-an-`Error`-not-`Exception` convention verified live (09); `dart:io` file I/O (verified live to throw `PathNotFoundException` for a missing file, matching Kotlin/Java) and `dart:convert`'s `jsonEncode`/`jsonDecode` verified live as genuinely built-in JSON support, framed as a positive contrast since Java/Kotlin/C++/Rust all needed an external JSON library in their own courses (10); mixins via `with` — verified live to add a method to a class with zero inheritance relationship involved, Dart's own dedicated language construct distinct from both PHP's traits and Kotlin's protocol-extension pattern — plus named/factory constructors and the `fromJson`/`toJson` convention (11); extension methods (verified live on `String`, directly comparable to Kotlin's extension functions) and `typedef`-based function composition (12); a genuinely important, live-verified finding: **Dart generics are reified at runtime, not erased** (`is List<int>`/`is List<String>` correctly distinguish, `.runtimeType` shows the real type argument) — a real, checked contrast with Java's erasure-based generics covered earlier in this repository (13); native `Future`/`async`/`await` (no external library, matching Swift, contrasted with Kotlin's `kotlinx.coroutines`) with real measured concurrency timing (433ms sequential vs. 203ms concurrent), `Stream` for asynchronous sequences via `async*`/`yield`, and `Isolate`s — verified live via a spawned isolate computing and returning a sum through message passing, Dart's genuinely unique no-shared-memory parallelism model (14); Dart's underscore-based privacy verified live to be scoped to the *file*, not the class — a real, checked contrast with Java/Kotlin's class-based `private` — and `pubspec.yaml`/`dart pub` (15); no built-in database access (matching Swift/C++), using the `sqlite3` pub.dev package, verified live to work with zero system-level setup, plus the same SQL-injection-safety demonstration as every other language course (16); the built-in `dart:io HttpClient` against the same live `jsonplaceholder.typicode.com` API used throughout this repository, confirming no exception is thrown for a 404 (matching every language course except Rust's `ureq`) (17); the official `test` package, resolved via `dart pub get`, with Dart 3 record destructuring used for table-driven tests (18); and a best-practices synthesis reproducing three genuine anti-pattern/fix pairs live — `List == ` verified live to be **identity equality by default, not content equality** (`[1,2,3] == [1,2,3]` is `false`!), force-unwrap producing an unhelpful generic error versus a clear specific one, and heavy synchronous computation verified live (via a real, ticking `Stream.periodic` timer) to completely block the event loop, resuming only once the same work moved to a separate `Isolate` (19). Every example across all 19 lessons was actually run with the Dart SDK, output verified line-by-line against live network calls and real measured/observed timing where applicable, and all `.dart_tool` build caches cleaned up afterward (added to `.gitignore`). Lessons 20–22 are not built, consistent with every other language course.

- **`10-Functional-Programming/`** — complete. Module README plus 5 fully-built lessons (`01-Pure-Functions-and-Immutability` through `05-Currying-and-Partial-Application`), each with a full README (Beginner/Intermediate/Advanced sections, Real-World Usage, Summary, Key Terms, Common Mistakes, Interview Questions, Suggested Next Lesson) and a runnable, verified `example.py`. Covers: pure functions vs. side effects (including mutating an argument, not just a global), `tuple` immutability, and `functools.lru_cache` memoization only being safe because the cached function is pure (01); first-class functions, closures via a function-factory pattern, and a `@timed` decorator (02); `map()`/`filter()`/`functools.reduce`, their more idiomatic comprehension equivalents, and a live-verified demonstration that `map()`/`filter()` are lazy in Python 3 (a call-logging function proved zero elements were processed until the iterator was actually consumed) (03); `compose()` (right-to-left) vs. `pipe()` (left-to-right), verified live to produce identical results for correspondingly-reversed argument orders, plus a practical text-sanitizing pipeline (04); manual currying (a generic `curry3` helper) contrasted directly with `functools.partial`, verified live that `partial()` can fix multiple arguments in one call while a curried function cannot (05). Also includes [Exercises/](10-Functional-Programming/Exercises/README.md) (5 problems, one per lesson) and [Solutions/](10-Functional-Programming/Solutions/README.md) (5 matching worked solutions, each with a runnable, verified `.py` file and an explanatory `.md` answering the exercises' reflection questions), plus a [Mini-Project/](10-Functional-Programming/Mini-Project/README.md) — a sales-transaction analytics pipeline (`pipeline.py`/`data.py`/`report.py`) tying together all five lessons' concepts in one program, with a standalone `test_pipeline.py` (7/7 tests passed) verifying every pure function in isolation before they're combined. Every `.py` file across the entire module (lessons, exercises, solutions, mini-project) was actually executed and its real output verified line-by-line — no exceptions. This was undertaken as the next unclaimed item in ROADMAP.md's own stated Phase 2 order (Language → OOP → **Functional Programming** → DSA), since the original 12-language build order (see below) was already complete and the module was found completely empty on inspection.

- **`04-Backend-Development/`** — complete. Module README plus 5 fully-built lessons using **Java and Spring Boot** as the module's single reference stack (the same reasoning as Python for `09-Object-Oriented-Programming`/`10-Functional-Programming` — see the module README's explicit rationale, including why Node.js/Express was deliberately not built as a separate lesson). The Maven (3.9.16) + Spring Boot (4.1.0, the current major version as of this build) toolchain was not present in this environment and was set up fresh: Maven via a direct zip download into a scratchpad-local directory (the same self-contained pattern used for every language toolchain this repository has installed), and Spring Boot projects scaffolded via the real `start.spring.io` Initializr API. Every lesson's server was started via a precisely PID-tracked process (captured via PowerShell `Start-Process -PassThru`, verified via `Get-CimInstance`/command-line inspection before being stopped by exact PID — never a blanket `taskkill` by image name, out of care for other unrelated processes on the shared machine) and exercised with real `curl` requests before being cleanly shut down; `mvn test` was run to completion for the one lesson built around automated tests. Specific content: a REST API built with **zero framework** — only the JDK's built-in `com.sun.net.httpserver` — demonstrating the four REST fundamentals (resources/URIs, HTTP-method-to-CRUD mapping, status codes, statelessness) with every status code (200/201/204/400/404/405) verified live via real HTTP requests (01); the same API rebuilt with Spring Boot (`@RestController`/`@GetMapping`/etc., `ResponseEntity`, Java records as JSON DTOs), full CRUD verified live including automatic JSON (de)serialization (02); Spring Data JPA persistence via an embedded H2 database — `@Entity`, `JpaRepository` (zero implementation code), and a derived query method (`findByDoneFalse`) whose generated SQL was confirmed live both by its filtering behavior and by real `Hibernate: create table ...` SQL appearing in the application's own startup log (03); Spring Security + JWT authentication and `@PreAuthorize`-based role authorization, with a genuinely important, live-verified finding that unauthenticated requests return `403` (not the commonly-assumed `401`) under Spring Security's actual default configuration, and a full authenticate-as-USER-succeeds/authenticate-as-USER-but-wrong-role-fails/authenticate-as-ADMIN-succeeds sequence proving authentication and authorization are genuinely separate, independently-checked concerns (04); and `@WebMvcTest`/`@MockitoBean`-based unit tests alongside `@SpringBootTest`-based integration tests against a real database, which surfaced and required working through **three real, verified Spring Boot 4.x breaking changes** discovered by directly inspecting resolved JAR contents after real compile/runtime failures (not assumption): `WebMvcTest` and `TestRestTemplate` each moved to their own new, separate Maven modules (`spring-boot-webmvc-test`, `spring-boot-resttestclient`) with `TestRestTemplate`'s package also changing; Spring Boot's own `@MockBean` was removed entirely in favor of Spring Framework's native `@MockitoBean`; and `spring-boot-resttestclient` turned out to need a further, non-transitive dependency (`spring-boot-restclient`, for `RestTemplateBuilder`) not declared by its own POM — all three fixed and confirmed via a final, fully green `mvn test` run (`Tests run: 10, Failures: 0, Errors: 0`, `BUILD SUCCESS`) (05). All `target/` build directories were cleaned up after each lesson's verification. Lessons 20-22-style standalone exercises/mini-projects were not built for this module, consistent with this repository's per-concept-module (rather than per-language-course) pattern.

- **`07-Databases/`** — complete. Module README plus 6 fully-built lessons, using **Java with plain JDBC** for Lessons 01-04 (deliberately not an ORM, so raw SQL stays visible), **Hibernate/JPA** for Lesson 05, and the **MongoDB Java driver against a real, embedded `mongod`** (via `de.flapdoodle.embed.mongo` 4.18.1 + `mongodb-driver-sync` 5.2.1, verified genuinely working in this Windows sandboxed environment with no separate MongoDB install) for Lesson 06. Every lesson was compiled and run via `mvn compile exec:java` against a real H2 database engine, with real output copied into each lesson's README. Specific content: full CRUD + `INNER JOIN` via `PreparedStatement`/`Statement.RETURN_GENERATED_KEYS`, plus a genuine H2 reserved-keyword syntax error (`YEAR`) hit and fixed live (01); a live-reproduced update anomaly — an unnormalized schema allowed the same customer to end up with two conflicting emails after an incomplete `UPDATE`, fixed by normalizing into `customers`/`products`/`orders` tables with foreign keys, with the same update then propagating correctly everywhere (02); all four ACID properties verified with real, observable effects — a `CHECK (balance >= 0)`-violating transfer correctly rolled back leaving both balances unchanged (Atomicity/Consistency), an uncommitted change confirmed invisible to a second real JDBC connection until commit (Isolation), and a value confirmed to survive a full connection close/reopen against a file-based (not in-memory) H2 database (Durability) — plus a second genuine reserved-keyword error (`VALUE`) hit and fixed live (03); a measured, not assumed, query speedup — `EXPLAIN` showing a real `tableScan` before an index and a real index-based plan after, with wall-clock timing on 200,000 batch-inserted rows showing a real 73ms→1ms improvement (04); JPA/Hibernate one-to-many (`Author`→`Book`) and many-to-many (`Book`↔`Tag`, auto-generated join table) relationships, plus the N+1 query problem reproduced with real `hibernate.show_sql` SQL-log output (1 query + N per-author lazy-loads) and fixed with `JOIN FETCH` (confirmed down to exactly 1 query) (05); and a real embedded MongoDB instance demonstrating schema flexibility (three same-collection documents with genuinely different fields, no migration needed), embedding vs. joins (a book with embedded reviews returned in one query), and `$gt`/`$set`/delete operations, all verified against real MongoDB server log output (06). This module directly extends and is cross-referenced with `04-Backend-Development` Lesson 03's Spring Data JPA work, going one level lower to show the `EntityManager`/JPQL foundation Spring Data JPA is built on.

- **`11-Design-Principles/`** — complete. Module README plus 4 fully-built lessons, using **plain Java** (no build tool or dependencies — each lesson is a single, self-contained `Example.java` compiled and run directly via `javac`/`java`). Every principle is demonstrated as a real, verified bug caused by violating it, followed by a fix, rather than described only in the abstract. Specific content: all five SOLID principles, each with a real violation and fix — a live-verified Liskov Substitution violation where substituting a `Square` for a `Rectangle` silently produced a wrong area (`got: 100` instead of the expected `50`), an Interface Segregation violation where a `RobotWorker` forced to implement a fat interface's `eat()` method threw a real exception at runtime, and a Dependency Inversion fix where the same `NotificationService` correctly sent both email and SMS purely by swapping an injected `MessageSender` implementation, with zero changes to `NotificationService` itself (01); DRY, KISS, and YAGNI, each with a live-verified real bug — a copy-pasted discount rule that actually drifted from 10% to 15% between two call sites, a "clever" bit-trick power-of-two check that was actually wrong for `n=0`, and a speculative, unused "future" tax strategy that contained a real, unnoticed calculation bug precisely because nothing ever exercised it (02); coupling and cohesion, each with a live-verified real bug — a `Display` class reading a `Thermometer`'s raw internal Fahrenheit field and mislabeling it as Celsius (98.6 shown as "98.6C" instead of the correct 37.0C), and a class mixing two unrelated responsibilities (header formatting, discount calculation) that shared a mutable field, causing one operation to silently clobber the other's remembered state (03); and composition over inheritance, with a live-verified "fragile base class" bug — an `ElectricCar` inheriting `startEngine()` from `Vehicle` unchanged, producing the literal, nonsensical output "Vroom! Engine started." for a vehicle with no combustion engine — fixed via composition (an injected `PowerSource` interface), with a third `HybridPowerSource` variant added afterward with zero changes to `Vehicle` itself, directly extending Lesson 01's Open/Closed principle (04).

- **`12-Design-Patterns/`** — complete. Module README plus 6 fully-built lessons, using **plain Java** (no build tool or dependencies — each lesson is a single, self-contained `Example.java` compiled and run directly via `javac`/`java`), covering all three GoF categories (creational, structural, behavioral) with Enterprise patterns (Repository, DTO, MVC/MVVM) honestly left uncovered. Every pattern is demonstrated as a real, verified bug caused by NOT using the pattern, followed by a fix, directly extending `11-Design-Principles`. Specific content: Singleton, with a genuinely measured race condition — 10 real threads released simultaneously via a `CountDownLatch` against a naive lazy singleton actually created 6-9 distinct instances (verified across multiple runs via `System.identityHashCode()`), fixed with the initialization-on-demand holder idiom reliably producing exactly 1 (01); Factory Method and Abstract Factory, with a live-verified copy-paste-drift bug (an SMS request silently sent as an Email because a notification-creation branch was updated in one method but not another) and a live-verified mismatched-UI-family bug (independent flags producing a Dark-Button/Light-Checkbox mix), both fixed by centralizing creation (02); Builder, with a live-verified swapped-argument bug — two same-typed constructor `int` parameters silently swapped, compiling fine but producing a nonsensical "12x 2-inch pizza" instead of "2x 12-inch pizza," fixed with named builder methods (03); Adapter and Decorator, with a live-verified 100x under-charge (a forgotten dollars-to-cents conversion at one of two call sites: 19 cents charged instead of 1999) and a live-verified drifted coffee price (a duplicated sugar-pricing formula across subclasses producing $2.70 instead of the correct $2.75), both fixed via centralized conversion/composition (04); Observer, with a live-verified stale-data bug — a mobile display added as a field but never wired into a hard-coded notification method, silently never receiving price updates, fixed by notifying a uniform list of registered observers (05); and Strategy and Command, with a live-verified order-dependent branching bug (Canada taxed at the EU rate, 20% instead of 13%, because an `if`/`else if` chain's EU check accidentally matched first) and a live-verified wrong-undo bug (a light's brightness restored to a hardcoded 50 instead of its real previous value of 80), both fixed via direct strategy lookup and command objects that capture their own undo state (06). All `.class` files were cleaned up after each lesson's verification.

- **`13-Software-Architecture/`** — complete. Module README plus 4 fully-built lessons, using **plain Java** for Lessons 01/02/04 (single, self-contained `Example.java` files, no build tool) and the JDK's built-in `com.sun.net.httpserver.HttpServer`/`java.net.http.HttpClient` for Lesson 03 (the same zero-framework approach as `04-Backend-Development` Lesson 01), directly extending `11-Design-Principles` and `12-Design-Patterns` to whole-system scope. Every architectural style is demonstrated as a real, verified bug caused by NOT following its core discipline, followed by a fix. Specific content: Layered/N-tier, with a live-verified bug where a presentation layer calling the data-access layer directly (skipping the service layer) let a negative-quantity order (`-5x Widget`) reach storage unchecked, fixed by enforcing the layer boundary (01); Clean/Hexagonal Architecture, with a live-verified real limitation — a domain class hard-wired to a concrete infrastructure class could not be exercised under a different (promotional) rate scenario without editing the infrastructure class itself, fixed by introducing a `DiscountRatePort` owned by the domain, verified by swapping adapters ($90.00 standard vs. $80.00 promotional) with zero changes to the domain class (02); Microservices Fundamentals, with a live-verified overselling bug (direct shared-state access let stock go to `-5`) fixed with a GENUINE embedded HTTP server and real `java.net.http.HttpClient` network calls — an oversell attempt was verified to receive an actual `HTTP 409 Conflict` response, with a valid request correctly receiving `HTTP 200`, and the server cleanly stopped afterward (03); and Event-Driven Architecture Basics, with a live-verified reliability bug where a non-critical analytics failure, called synchronously and unguarded, prevented an order's "fully processed" confirmation from ever printing despite inventory and email having already succeeded — fixed with an `EventBus` that isolates each subscriber's exception individually, verified live to let the identical analytics failure occur while the order still completed successfully (04). All `.class` files were cleaned up after each lesson's verification.

- **`14-APIs-and-Integrations/`** — complete. Module README plus 5 fully-built lessons, using **plain Java** (single, self-contained `Example.java` files, no build tool), with Lessons 01/02/05 using the JDK's built-in `com.sun.net.httpserver.HttpServer`/`java.net.http.HttpClient` for genuine embedded-server network verification (the same zero-framework approach as `13-Software-Architecture` Lesson 03), and Lesson 03 using the JDK's own `javax.crypto` for a genuinely working HMAC-SHA256-signed token. Specific content: HTTP Fundamentals/idempotency, with a live-verified bug where retrying a non-idempotent `POST /orders` request against a real server created 2 separate orders, fixed with an idempotent `PUT` (client-supplied ID) leaving exactly 1 order after the identical retry (01); REST Design and Versioning, with a live-verified breaking-change bug — an unversioned field rename (`price`→`unitPrice`) made a real "old client" parser return `null` — fixed with coexisting `/v1/`/`/v2/` endpoints, verified live to let both an old and new client succeed simultaneously (02); Authentication, with a live-verified authorization-bypass bug where a genuinely, cryptographically valid (HMAC-signed) read-only token was wrongly accepted for a destructive delete operation because only signature validity was checked, not scope — fixed with an explicit scope check, verified live to correctly reject the same token while a write-scoped token succeeded, and a genuinely tampered token was separately confirmed still caught by signature verification (03); API Documentation, with a live-verified real `NullPointerException` crash caused by a documented-but-unenforced required field, fixed by validating requests against the contract before business logic runs, verified live with a clear, specific rejection message (04); and Consuming Third-Party APIs, with real, measured elapsed times against a genuinely slow embedded server proving a timeout's effect — a client with no configured timeout took a real 3211ms, while a client with an explicit 1-second timeout failed fast at a real 1003ms via `HttpTimeoutException` (05). All `.class` files were cleaned up after each lesson's verification.

- **`15-Testing-and-Debugging/`** — complete. Module README plus 5 fully-built lessons, using plain **JUnit 5** via Maven for Lessons 01-04 (distinct from `04-Backend-Development` Lesson 05's Spring Boot test annotations) and a single self-contained `Example.java` for Lesson 05. Every testing layer is demonstrated against a real, pre-existing bug that layer's technique genuinely caught, with actual `mvn test` output captured at every stage — not illustrative/hypothetical output. Specific content: Unit Testing, with a real `PriceCalculator.applyDiscount()` percentage bug caught live by 2 of 3 JUnit tests failing with wildly wrong values (`expected: <80.0> but was: <-1900.0>`), fixed and reverified with `Tests run: 3, Failures: 0` (01); Integration Testing, with a real unflushed/unclosed `FileWriter` bug caught live by a `@TempDir`-based test against a REAL file (`expected: <Buy milk> but was: <>`, plus a genuine Windows `FileSystemException` file-lock error during cleanup), fixed with try-with-resources and reverified green — explicitly demonstrated as a bug a mocked file system could never have caught (02); End-to-End Testing, with a real routing/wiring bug (a correctly-implemented handler registered under the wrong URL path) caught live only by starting the REAL app on a REAL OS-assigned port and hitting the documented endpoint (`expected: <200> but was: <404>`), fixed and reverified green — explicitly demonstrated as invisible to both unit and typical integration tests (03); Test-Driven Development, with THREE genuine, real red→green cycles captured via actual `mvn test` output building up a `PasswordStrengthValidator` one requirement at a time (length, then digit, then uppercase), each new test verified to genuinely fail for the expected reason before its corresponding minimal implementation was written (04); and Debugging Techniques, with a real `ArrayIndexOutOfBoundsException` (from a genuine off-by-one loop bound) read and interpreted via its actual stack trace, plus a real silent logic bug (a shared `static` accumulator contaminating two "accounts'" totals) diagnosed live via one targeted diagnostic log line that immediately proved the root cause, both then fixed and reverified correct (05). All `target/`/`.class` files were cleaned up after each lesson's verification.

- **`16-Security/`** — complete. Module README plus 4 fully-built lessons, each executing a REAL, working exploit against genuinely vulnerable code followed by a verified fix — not a hypothetical description. Lesson 01 reuses the H2 pattern from `07-Databases`; Lessons 03/04 reuse the real embedded HTTP server pattern from `13-Software-Architecture`/`14-APIs-and-Integrations`; Lesson 02 uses the JDK's own `javax.crypto`; Lesson 04 uses a genuine `keytool`-generated self-signed certificate with `HttpsServer`/`SSLContext` for a real TLS handshake. Specific content: SQL Injection, with a real, successful `' OR '1'='1' --` authentication bypass against a real H2 database (verified by the actual executed SQL string and a real `true` login result with no valid password), fixed with `PreparedStatement` verified to correctly reject the identical payload while a genuine valid login still succeeded (01); Secure Password Storage, with real MD5 hashes proving identical passwords produce identical hashes (a real information leak) and real measured timing showing PBKDF2 roughly 86,000x more expensive per hash than MD5 (100,000 MD5 hashes in 211ms vs. 20 PBKDF2 hashes at 120,000 iterations in 3,642ms), fixed with per-user random salts verified to produce completely different hashes for identical passwords while still correctly verifying a genuine password (02); Input Validation and Output Encoding, with a real, literal, executable `<script>alert('XSS')</script>` tag verified present in an actual HTTP response body, fixed with HTML entity encoding verified to neutralize the identical payload into inert text in the real response (03); and HTTPS and Security Headers, with a genuinely real TLS 1.3 handshake (verified via the actual negotiated `TLSv1.3`/`TLS_AES_256_GCM_SHA384` printed from a real `SSLSession`) against a real `keytool`-generated self-signed certificate, plus real HTTP responses verified missing `X-Content-Type-Options`/`X-Frame-Options` in the violation and correctly including them (plus CSP and HSTS) in the fix (04). Added `*.jks` to the repository `.gitignore` for Lesson 04's throwaway keystore. All `.class`/`target/`/`.jks` files were cleaned up after each lesson's verification.

- **`17-Git-and-GitHub/`** — complete. Module README plus 5 fully-built lessons. Unlike this repository's other concept modules, Git itself is the subject rather than a programming language used to build examples — every lesson includes a `demo.sh` script that was actually run end-to-end in a throwaway directory, with its real captured terminal output included in that lesson's README (not a description of expected behavior). Specific content: Core Commands, with a real, verified demonstration that `git commit` only includes staged changes — an unstaged modification correctly caused git to refuse the commit outright (`no changes added to commit`), confirmed via `git log` showing no new commit was created, then correctly succeeding once staged (01); Branching and Merging, with a REAL merge conflict reproduced end-to-end — two branches modifying the same line differently produced an actual `CONFLICT (content)` error and real `<<<<<<<`/`=======`/`>>>>>>>` markers in the file, resolved and verified via a real two-parent merge commit in `git log --graph` (02); Rebase and Cherry-pick, with real commit hashes proving rebase creates brand-new commits (the same content changes had genuinely different hashes before vs. after rebasing), and a real cherry-pick verified by actual file-level presence/absence (`patch.txt` present, `wip.txt` correctly absent after cherry-picking only one of two commits) (03); Pull Requests and Code Review, honestly demonstrating the underlying git mechanics a PR is built on (branch diffing, addressing review feedback with a real fixup commit, squashing into a clean commit before merge) since no GitHub remote/account is configured in this environment — verified via real `git diff`/`git log` output at each stage, including a real, deliberately introduced typo caught and fixed (04); and GitHub Actions Basics, with a real, valid workflow YAML file verified two genuine ways — parsed successfully with `pyyaml`, surfacing a real, documented YAML 1.1 gotcha (the bare `on:` key parsing as the boolean `True`, not the string `"on"`), and its actual `run: mvn test` step verified to genuinely succeed by running the identical command against a real Maven project reused from `15-Testing-and-Debugging` Lesson 01 (05).

- **`06-Desktop-Development/`** — complete. Module README plus 4 fully-built lessons using **JavaFX 26.0.1** (added as a real Maven dependency + `javafx-maven-plugin`, downloaded fresh into the local `.m2` cache since JavaFX is no longer bundled with the JDK past Java 10; JavaFX SDK zip was also separately downloaded into a scratchpad directory for an initial standalone compatibility check against JDK 25). With the user's explicit approval (via `AskUserQuestion`), every lesson launches a GENUINELY REAL window — actually rendered on the user's real screen for a few seconds — then closes itself programmatically, extending this repository's "verify by actually running it" discipline into a visual/GUI context. Specific content: JavaFX Fundamentals (Stage/Scene/layout/controls), with a real window shown and three button clicks fired programmatically (`clickButton.fire()`), verified via a printed, passing assertion on the resulting label text (01); Data Binding and Observable Collections, with a live-verified real stale-display bug — a label manually synced only via a mouse-drag handler stayed stuck at "Value: 50" after a programmatic `slider.setValue(20)` call, since `setValue()` never fires a drag event — fixed with real property binding (`textProperty().bind(...)`) verified to update correctly under the identical scenario, plus a real `ObservableList` shown to keep a `ListView` automatically in sync with zero manual refresh code (02); JavaFX Threading Rules, with a genuinely surprising, VERIFIED finding discovered through direct experimentation (not assumed from docs): updating a `Label` from a background thread threw a real `IllegalStateException`, but NOT at the `setText()` call site — the exception was thrown asynchronously from the control's internal listener chain and only surfaced via the background thread's uncaught-exception handler, meaning a normal `try`/`catch` around the call could never have caught it; also verified that the underlying property value changed anyway despite the exception (a genuinely unsafe, undefined-behavior state) — fixed correctly with `Platform.runLater()` (03); and Building a CRUD Desktop App, a complete, real Task Manager with a property-based `Task` model, a real file-backed `TaskRepository`, and a `TableView` UI — every CRUD operation (Create/Read/Update/Delete) was exercised via real button `fire()` calls and verified against real `TableView`/list state, and persistence was verified with a genuine round-trip: saving to a real temp file, then reloading via a BRAND-NEW `TaskRepository` instance sharing no in-memory state, confirmed correct via the actual raw file contents and the reloaded task's name/done-status matching (04). All `target/` directories were cleaned up after each lesson's verification. Note: this module was built after an attempt to build `18-DevOps-and-Cloud` first — Docker Desktop is installed on this machine but its backend would not come up (diagnosed as misconfigured for Windows-containers mode via `settings-store.json`'s `UseWindowsContainers: true`; switching it and restarting Docker Desktop did not resolve the underlying IPC/backend issue). The user declined further troubleshooting, so `18-DevOps-and-Cloud` was intentionally left unbuilt for a future session with working Docker access, and this module was built instead.

- **`05-Mobile-Development/`** — complete. Module README plus 6 fully-built lessons using **native Android with Java** (Gradle 8.2 + AGP 8.2.2 + JDK 17 — a distinct toolchain from this repository's usual JDK 25 Java modules, since current Android tooling requires it), chosen over the tracker's suggested Flutter/Dart or React Native given this session's Java-preferred directive and the discovery that a full Android SDK, multiple AVDs, and Gradle were ALREADY installed and working in this environment (WHPX hardware acceleration confirmed via `emulator -accel-check`). Every lesson was built into a real APK, installed on a genuinely real, running AVD (`Medium_Phone_API_28`), and driven with real `adb` commands (taps at coordinates the app itself logged from its own real layout, typed text, process kills) — verified via real `adb logcat` output AND real screenshots (visually inspected via the Read tool, not merely captured). Specific content: Android Fundamentals, with the complete, real Activity lifecycle sequence captured live via logcat across a real launch, backgrounding (`adb shell input keyevent KEYCODE_HOME`), and return to foreground — confirming `onStop` does not mean destroyed (the same Activity instance resumed via `onRestart`) (01); Layouts and Views, with three genuine physical taps on a real screen (coordinates read from the app's own logged, laid-out button position) each correctly incrementing a counter, verified via both logcat and a real screenshot (02); RecyclerView and Adapters, with a live-reproduced real stale-list bug (modifying a backing list without notifying the adapter left a real screenshot showing only 3 of 4 actual items) and a genuinely surprising, unplanned finding discovered through direct experimentation: a single `notifyItemInserted()` call caused the RecyclerView to catch up on ALL previously-unnotified changes at once (both the stale item AND the new one appeared together), revealing that notify calls are a "re-check" signal, not a narrowly-scoped update (03); Local Storage with Room, with the strongest possible persistence proof — a real note saved to a real Room/SQLite database, the app's ENTIRE PROCESS then genuinely killed (`adb shell am force-stop`, confirmed via `pidof` returning nothing), and a relaunch (with a verified NEW process ID) correctly reloading the note from disk (04); REST API Integration, with a real network call from the Android emulator to a real JDK `HttpServer` running on the HOST machine via the emulator's `10.0.2.2` alias — verified in BOTH a real failure state (genuine `SocketTimeoutException` before the host server started) and a real success state (`HTTP 200` with real JSON), independently confirmed by the SERVER's own separate log showing it served the request (05); and Building a CRUD Mobile App, a complete capstone Task Manager combining Room + RecyclerView + real UI, with Create and Delete each verified via real taps/logs/screenshots, and the full CRUD lifecycle chained together with a final process-kill persistence proof confirming exactly the surviving task (after one add-two/delete-one sequence) correctly reloaded in a brand-new process (06). All `app/build/`/`.gradle/` directories were cleaned up, and all six lessons' test apps were uninstalled from the emulator, after verification. Building this module required real environment diagnosis: `assembleDebug` initially failed under JDK 25 (Gradle/AGP requires JDK 17) and then failed again with a missing `android.useAndroidX=true` gradle.properties setting — both real, fixed blockers, not assumed configuration.

## In Progress

None at time of writing.

## Repository Hygiene

A repo-wide `find . -type d -empty` sweep this session found and removed four genuinely vestigial empty directories (none contained even a `.gitkeep`, and git doesn't track empty directories regardless, so nothing was ever going to be lost): `00-Programming-Fundamentals/Exercises` and `.../Solutions` (redundant top-level scaffolding — every one of that module's 8 lessons already has its own per-lesson `Exercises/`/`Solutions/`, verified populated); and `22-Projects/Beginner/Task-Management-CRUD/docs` and `.../vanilla-html-js-css` (leftover from earlier planning — the project's actual docs live at its root as `ARCHITECTURE.md`/`TESTING.md`, and its actual vanilla-JS implementation lives directly at the project root, not inside a `vanilla-html-js-css` subfolder). Also found and removed two lingering `.NET` build-artifact trees (`bin/`/`obj/` under `01-Languages/CSharp/15-Modules-and-Packages` and `.../18-Testing`) that had been left on disk despite being `.gitignore`d — regenerable via `dotnet build`/`dotnet test`, not source content. A second sweep confirmed the repository now has zero stray empty directories outside the 19 root modules that are entirely unbuilt by design (03, 04, 05, 06, 07, 11–21, 23–25 — see Remaining Sections below).

## Remaining Sections (Not Started / Scaffolded Only)

- `01-Languages/` — Python, JavaScript, TypeScript, C#, Java, C++, Go, Rust, PHP, Kotlin, Swift, and Dart now all have their full core courses (lessons 01–19 or equivalent — see Completed Sections). **This completes the original brief's full specified 12-language build order.** Swift's course remains the one disclosed exception to this repository's "verified by execution" discipline (see its entry above) — re-verifying it against a real Swift toolchain, if one becomes available, would be high-value follow-up work. All twelve language courses are missing lessons 20–22 (standalone Exercises/Solutions/Mini-Projects folders). Every other language in the original spec (C, Ruby, Haskell, Scala, R, MATLAB, Lua, Perl, Bash, PowerShell) plus low-level/systems topics remain entirely unbuilt.
- `08-Data-Structures-and-Algorithms/` — already has an introductory module complete (see the existing entry further up this file); Trees, Heaps, Graphs, Dynamic Programming, Greedy Algorithms remain honestly-labeled planned stubs. This is the natural next step per ROADMAP.md's Phase 2/3 ordering (Language → OOP → Functional Programming → **DSA**).
- Every other non-language root module (`02-Markup-and-Styling`'s remaining topics, `03-Frontend-Development`, `18-DevOps-and-Cloud` through `21-Interview-Preparation`, `22-Projects` beyond the one existing CRUD app, `23-Cheat-Sheets`/`24-Exercises`/`25-Solutions` as cross-cutting collections) remains entirely or mostly unbuilt. `18-DevOps-and-Cloud` specifically is blocked on a non-functional Docker Desktop backend on this machine (see `06-Desktop-Development`'s Completed Sections entry) — worth retrying in a future session, potentially in a different environment or after manual Docker repair.
- `02-Markup-and-Styling/` — HTML5 and CSS3 are done (see Completed Sections above); still missing: XML, JSON, YAML, Markdown, SVG, JSX/TSX, Sass/SCSS/Less, Tailwind CSS, Bootstrap, Material Design, and CSS methodologies (BEM/OOCSS/SMACSS/CSS Modules/CSS-in-JS).
- `03-Frontend-Development/`, `05-Mobile-Development/`, `06-Desktop-Development/`
- `08-Data-Structures-and-Algorithms/` — Trees, Heaps, Graphs, Dynamic Programming, Greedy Algorithms (currently README-only stubs, honestly labeled as planned).
- `10-Functional-Programming/` through `21-Interview-Preparation/`
- `22-Projects/` — Intermediate and Advanced projects, and additional stack variants of the Task Management CRUD app.
- `23-Cheat-Sheets/`, `24-Exercises/`, `25-Solutions/` as cross-cutting collections (currently only per-module exercises/solutions exist).
- `SKILL_ASSESSMENT.md` (referenced by `STUDY_PLAN.md`, not yet created).

## Examples Tested

Every runnable file in the five completed sections above (`00-Programming-Fundamentals`, `01-Languages/Python`, `09-Object-Oriented-Programming`, `08-Data-Structures-and-Algorithms`, and the Task Management CRUD project) was actually executed — with `python` for `.py` files and `node` for the CRUD project's pure-logic tests — and its output verified against what's documented, not assumed. Mismatches found during authoring (quoting differences, non-deterministic race-condition demos, dry-run traces not matching real instrumented output, two real CRUD logic bugs) were fixed before being marked complete. All `__pycache__`/`.pytest_cache` artifacts created during testing were removed; a root `.gitignore` now prevents them from being tracked going forward.

The `02-Markup-and-Styling` HTML/CSS files have no interpreter to "run" the way a script does; instead, all 5 `.html` files were validated with Python's `html.parser` (zero unclosed/mismatched tags) and all 3 `.css` files had brace-balance verified. No browser was available in this environment to visually confirm rendering — a human should open `Mini-Project/index.html` in an actual browser and check the dark-mode toggle and Grid reflow behavior described in its README before fully trusting the visual claims.

All 30 `.js`/`.mjs` files across the full `01-Languages/JavaScript` course (lessons 01–19: 19 lesson examples, 8 exercise solutions, 2 module-system demo files, plus the `math.js`/`math.test.js` pair for lesson 18) were executed with `node` (v24.12.0 in this environment; lesson 18 via `node --test`, all 7 tests passing) and their real output compared line-by-line against what each README/solution doc claims. This caught several real bugs during authoring, not after: a hand-computed (rather than run) "Fizz" count of `2` in the Control Flow solution writeup, corrected to the actual verified count of `4`; a literal unescaped double-quote inside a string in the Generics lesson's `example.js`, which was a genuine `SyntaxError` until fixed; and a hand-guessed (wrong) property breakdown in that same lesson's README for what `{...spread}` of the string `"oops"` actually produces, corrected to match the real executed output. Lesson 14's concurrency claim (`Promise.all` beats sequential `await`) and Lesson 19's best-practice contrast (a `||`-based default silently breaking on an intentional `0`) were both verified with real `Date.now()` timing / real executed output, not just asserted in prose. No `npm install` was needed anywhere in lessons 01–19 — lessons 16–18 deliberately use only Node's built-in `node:sqlite`, global `fetch`, and `node:test` instead of third-party packages, specifically to keep the whole course install-free.

## Known Limitations

- This is a solid foundation across five modules, not the entire spec — the original brief describes a repository with many thousands of files across dozens of languages and frameworks, which cannot be produced in a single pass. This build prioritizes **depth and correctness over breadth**, per the brief's own instruction ("prioritize quality over generating thousands of nearly empty files").
- Interview-question counts (100/150/etc. per category) have not been generated yet — only the per-lesson interview questions embedded in each README exist so far.
- No CI/CD, Docker, or cloud deployment configuration has been created yet.
- Mermaid diagrams exist only in the OOP module and the CRUD project so far.
- `01-Languages/Python/17-API-Integration` depends on a live public test API being reachable; if network access is unavailable in a future environment, that lesson's example may need to be re-verified or adapted to a mocked response.

## How To Resume

Follow [CONTRIBUTING.md](CONTRIBUTING.md) conventions. Work module-by-module in the order given in [ROADMAP.md](ROADMAP.md), though this session has been directed by the user to prioritize filling in this repository's remaining **empty root modules using Java as the preferred implementation language** wherever a language choice is needed — this directive currently supersedes strict ROADMAP ordering for module selection. **The full, originally-specified 12-language build order is complete** (Python through Dart, core lessons 01–19 each), **`10-Functional-Programming` is complete** (5 lessons + Exercises/Solutions + Mini-Project), **`04-Backend-Development` is complete** (5 lessons using Java/Spring Boot, all verified — see its Completed Sections entry for the real Spring Boot 4.x breaking changes discovered and fixed along the way), **`07-Databases` is complete** (6 lessons using Java/JDBC, Hibernate/JPA, and embedded MongoDB, all verified — see its Completed Sections entry), **`11-Design-Principles` is complete** (4 lessons in plain Java, all verified — see its Completed Sections entry), **`12-Design-Patterns` is complete** (6 lessons in plain Java covering all three GoF categories, all verified — see its Completed Sections entry), **`13-Software-Architecture` is complete** (4 lessons, including a genuine embedded-HTTP-server microservices demo, all verified — see its Completed Sections entry), **`14-APIs-and-Integrations` is complete** (5 lessons, including real embedded-HTTP-server idempotency/versioning/timeout demos and a genuinely HMAC-signed token, all verified — see its Completed Sections entry), **`15-Testing-and-Debugging` is complete** (5 lessons using plain JUnit 5, each demonstrating a real bug genuinely caught by that lesson's testing layer, with actual `mvn test` output at every stage — see its Completed Sections entry), **`16-Security` is complete** (4 lessons, each executing a real, working exploit — a genuine SQL injection auth bypass, a real ~86,000x measured password-hashing cost difference, a real injected `<script>` tag, and a real TLS 1.3 handshake — see its Completed Sections entry), **`17-Git-and-GitHub` is complete** (5 lessons, each running real git commands in a throwaway repository with real captured output, including a real merge conflict, real changed commit hashes from rebase, and a real GitHub Actions YAML file verified both structurally and functionally — see its Completed Sections entry), **`06-Desktop-Development` is complete** (4 lessons using real JavaFX 26.0.1, with a genuinely real window shown on screen for every lesson, verified live — see its Completed Sections entry), and **`05-Mobile-Development` is now also complete** (6 lessons using native Android/Java on a genuinely real, running emulator, with real taps/screenshots/process-kill persistence proofs at every stage — see its Completed Sections entry). `18-DevOps-and-Cloud` was attempted but blocked by a non-functional Docker Desktop backend on this machine (see that entry for the diagnosed root cause) — the user declined further troubleshooting, so it remains unbuilt. **Swift remains the one exception to this file's "verified by execution" discipline** — see its Completed Sections entry for the full, honest account of why. **If a future session has access to a real Swift toolchain, verifying every Swift lesson's example remains the single highest-value piece of unfinished-by-necessity work.**

**Per the user's explicit direction this session, the remaining empty root modules should be filled in next, using Java wherever a language choice is needed:** `03-Frontend-Development`, `18-DevOps-and-Cloud`, `19-Command-Line-and-Operating-Systems`, `20-Computer-Science-Fundamentals`, `21-Interview-Preparation`, `23-Cheat-Sheets`, `24-Exercises`, `25-Solutions` are all still entirely empty (`07-Databases`, `11-Design-Principles`, `12-Design-Patterns`, `13-Software-Architecture`, `14-APIs-and-Integrations`, `15-Testing-and-Debugging`, `16-Security`, `17-Git-and-GitHub`, `06-Desktop-Development`, and `05-Mobile-Development` are now done — see their Completed Sections entries). `18-DevOps-and-Cloud` was attempted this session but is BLOCKED: Docker Desktop is installed on this machine but its backend will not start correctly (diagnosed as stuck/misconfigured — see `06-Desktop-Development`'s entry for the specific symptoms and what was tried); the user declined further troubleshooting, so this needs either a working Docker environment in a future session or manual repair on the user's end before it can be built with real, verified `docker` commands. The Android SDK/emulator toolchain used for `05-Mobile-Development` (JDK 17, Gradle 8.2, AGP 8.2.2, AVD `Medium_Phone_API_28`) is now verified-working in this environment and can be reused directly for any future Android-related work. `03-Frontend-Development` is a partial exception — genuinely frontend topics (DOM, Fetch API, React) are inherently JavaScript/TypeScript-based (this repository's existing JS/TS courses already cover the language fundamentals), so "Java-preferred" there would mean framing it around a Java-backend-serving-a-frontend angle, or simply building it in JS/TS as the domain requires; use judgment and consider asking if genuinely unclear. `08-Data-Structures-and-Algorithms` also still has Trees/Heaps/Graphs/Dynamic-Programming/Greedy-Algorithms as honestly-labeled planned stubs (its introductory portion, in Python, is already complete) — worth revisiting if the user's Java-empty-folder directive is later relaxed back to strict ROADMAP order. When starting a new Maven/Spring Boot-based module, reuse the already-verified Maven 3.9.16 install and the PID-tracked start/verify/stop pattern established in `04-Backend-Development` (documented in that module's lessons) rather than re-deriving it. Given the sheer size of the full spec (dozens of languages/frameworks across 25 root modules, thousands of interview questions, a dozen full projects), no single session will complete "everything" — this file's job is to make each session's real, verified progress legible so the next one can continue without re-deriving state. When delegating work (e.g. to a subagent), always verify claimed completions against the actual filesystem before trusting them — this file has been wrong before, and note that Swift specifically has NOT been verified by execution at all, unlike every other course.
