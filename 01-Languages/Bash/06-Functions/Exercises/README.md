# Exercises — Functions

1. Write a function `max_of` that takes two numbers and prints (not returns) the larger one; capture its output with command substitution and print "Max: N".
2. Write a function `is_palindrome` that takes a string and returns exit code 0 if it reads the same backward, 1 otherwise (use it in an `if`).
