# Exercises — Arrays

1. Given an indexed array of numbers, compute and print the sum and integer average.
2. Given an indexed array of words (with duplicates), use an associative array to count occurrences of each word and print `word: count` for each.
