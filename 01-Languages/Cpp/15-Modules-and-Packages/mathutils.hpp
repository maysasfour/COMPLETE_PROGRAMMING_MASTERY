// mathutils.hpp - a header declaring what mathutils.cpp implements.
// Included by any .cpp file that wants to call these functions.
#pragma once // include guard: prevents this header's contents being pasted in twice into one translation unit

namespace mathutils {
    int add(int a, int b);
    int multiply(int a, int b);
}
