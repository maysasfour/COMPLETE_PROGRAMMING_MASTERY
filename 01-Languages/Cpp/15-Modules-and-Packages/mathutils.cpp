// mathutils.cpp - implements what mathutils.hpp declares. Compiled as its own translation unit.
#include "mathutils.hpp"

namespace mathutils {
    int add(int a, int b) { return a + b; }
    int multiply(int a, int b) { return a * b; }
}
