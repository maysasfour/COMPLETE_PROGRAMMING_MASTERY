// Example.kt - Kotlin compiles to JVM bytecode (also supports Kotlin/Native and Kotlin/JS,
// not covered here), so it runs on the JVM alongside this repository's Java course, and can
// interoperate with Java libraries directly.

fun main() {
    println("Hello, Kotlin!")
    println("Kotlin version: " + KotlinVersion.CURRENT)
    println("Running on the JVM, like this repository's Java course, but with a very different syntax.")
}
