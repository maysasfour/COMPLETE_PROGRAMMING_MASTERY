// Exercise: FizzBuzz using `when` as an expression (not a statement).
// Write a function fizzbuzz(n: Int): String returning "FizzBuzz"/"Fizz"/"Buzz"/the number,
// using `when` with boolean conditions (a bare `when { condition -> result }`, no subject).

fun fizzbuzz(n: Int): String {
    // TODO: implement using when { n % 15 == 0 -> ..., ... }
    return ""
}

fun main() {
    for (i in 1..15) println(fizzbuzz(i))
}
