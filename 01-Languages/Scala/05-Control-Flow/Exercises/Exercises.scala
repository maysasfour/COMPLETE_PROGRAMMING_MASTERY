// Exercises.scala - 05 Control Flow
//
// 1. Write `fizzbuzz(n: Int): String` returning "Fizz" for multiples of 3, "Buzz" for
//    multiples of 5, "FizzBuzz" for multiples of both, else the number as a string.
//    Implement it using `match` with guards.
//
// 2. Write `gradeOf(score: Int): String` using `match` with numeric-range guards,
//    returning "A" (>=90), "B" (>=80), "C" (>=70), else "F".
//
// 3. Using a `for`-comprehension with `yield`, produce a `List[Int]` of the squares
//    of only the even numbers from 1 to 10.
//
// Fill in the TODOs below, then compile and run against Solutions/Solutions.scala.

def fizzbuzz(n: Int): String =
  ??? // TODO

def gradeOf(score: Int): String =
  ??? // TODO

def evenSquares(): List[Int] =
  ??? // TODO

@main def exercisesDemo(): Unit =
  println("Implement the TODOs above, then compare against Solutions/Solutions.scala")
